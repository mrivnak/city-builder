use bevy::prelude::*;

use crate::components::TileBundle;

use super::terrain::TerrainNode;

// terrain form should already be generated, this just produces a tile to render
pub fn get_tile_from_terrain_node(node: TerrainNode) -> TileBundle {
    todo!()
}
